services:
  - type: web
    name: review-analyzer-backend
    runtime: python
    buildCommand: ""
    startCommand: python app.py
    rootDir: backend
    plan: free
    envVars:
      - key: PORT
        value: 5000
